export enum OutputType {
  shell = 'stateful.runme/shell-stdout',
  vercel = 'stateful.runme/vercel-stdout',
  vercelApp = 'stateful.runme/vercel-app',
  html = 'stateful.runme/html-stdout',
  error = 'error'
}
